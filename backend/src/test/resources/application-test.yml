spring:
  datasource:
    url: jdbc:h2:mem:soltest;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false
  flyway:
    enabled: false

sol:
  jwt:
    secret: test-secret-change-me-test-secret-change-me-test-secret-change-me
    expiration-minutes: 120
  cors:
    allowed-origins: http://localhost:5173
